version: 1.0
runtime: python3
build:
  commands:
    pre_build:
      - apt-get update && apt-get install -y libgtk-3-0 libnss3 libxss1 libasound2
    build:
      - pip install .
run:
  command: python main.py
  network:
    port: 8080